// Map mounts once in the root layout via <MapStage /> and stays alive across
// route changes. This route is intentionally empty so the persistent map shows
// through. See apps/web/src/components/map/MapStage.tsx.
export default function HomePage() {
  return null;
}
